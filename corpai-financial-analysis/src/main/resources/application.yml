spring:
  application:
    name: corpai-financial-analysis
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: corpai-financial-analysis
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "pl.pkobp.corpai.*"
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:corpai}
    username: ${DB_USER:corpai}
    password: ${DB_PASSWORD:corpai_secret}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    pl.pkobp.corpai: DEBUG
